import React, {Component} from 'react'
import {Image} from 'react-native'

// UI component used in login and signup screen
const StartScreenImage = () => (<Image source={require('./../assets/logo.png')}/>)

export default StartScreenImage;